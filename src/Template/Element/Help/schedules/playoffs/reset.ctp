<p><?= __('If you have initialized the schedule dependencies for the playoffs and need to completely remove them (rather than just recalculate them), use the "{0}" link from the division schedule page.',
	__('Reset Schedule Dependencies')
) ?></p>
